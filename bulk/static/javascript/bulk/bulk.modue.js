(function () {
    'use strict';

    angular
        .module('capvalue.bulk', [
            'capvalue.bulk.controllers',
            'capvalue.bulk.services'
        ]);

    angular
        .module('capvalue.bulk.controllers', []);

    angular
        .module('capvalue.bulk.services', []);

})();