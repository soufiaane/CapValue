(function () {
    'use strict';

    angular
        .module('capvalue.emails', [
            'capvalue.emails.services'
        ]);

    angular
        .module('capvalue.emails.services', []);
})();