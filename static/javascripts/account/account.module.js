(function () {
    'use strict';

    angular
        .module('capvalue.account', [
            'capvalue.account.controllers',
            'capvalue.account.services'
        ]);

    angular
        .module('capvalue.account.controllers', []);

    angular
        .module('capvalue.account.services', []);
})();